public Boolean isPrime(Integer num) {
    //code here

    Boolean primeFlag=false;
    for(Integer i=2;i<=(num/2);i++)
    {
        if(Math.mod(num,i)!=0)
        {

        }
        else
        {
            primeFlag=true;
            break;
        }
    }
    return primeFlag;
}